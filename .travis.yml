language: ruby
rvm:
  - 2.2.4
  - 2.3.0
script: bundle exec rspec -fp spec
sudo: false
before_install:
  - gem update --system
services:
  - redis-server
notifications:
  email:
    - andrew.kreps@gmail.com
